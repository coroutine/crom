# ------------------------------------------------------------------------------
# pkg - defines a namespace
# ------------------------------------------------------------------------------

pkg = (packageName) ->
  [path, obj] = [packageName.split('.'), window]
  _(path).each (pe) -> obj = (obj[pe] ||= {})
  obj
